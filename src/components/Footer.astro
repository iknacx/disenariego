---
import { Mail, Phone, MapPin } from "@lucide/astro";
import Logo from "../assets/images/logo.svg";

import { phone, email, address } from "../assets/data.json";
---

<footer class="relative z-10 bg-foreground/90 backdrop-blur-md text-primary-fg">
    <div class="container mx-auto px-4 py-16">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-12">
            <div>
                <div class="flex items-center gap-2 mb-4">
                    <Logo class="size-8 object-contain" />
                    <span class="font-display text-xl font-bold">
                        Diseña Riego
                    </span>
                </div>
                <p class="text-primary-fg/70 text-sm leading-relaxed">
                    Más de 30 años diseñando, instalando y manteniendo sistemas
                    de riego automático eficientes, confiables y sostenibles
                    para proyectos residenciales, comerciales e institucionales.
                </p>
            </div>
            <div>
                <h4 class="font-display text-lg font-semibold mb-4">Enlaces</h4>
                <div class="flex flex-col gap-2">
                    <a href="/">Inicio</a>
                    <a href="/about">Nosotros</a>
                    <a href="/services">Servicios</a>
                    <a href="/gallery">Galería</a>
                    <a href="/contact">Contacto</a>
                </div>
            </div>
            <div>
                <h4 class="font-display text-lg font-semibold mb-4">
                    Contacto
                </h4>
                <div class="flex flex-col gap-3 text-sm text-primary-fg/70">
                    <a href={`mailto:${email}`} class="flex items-center gap-2">
                        <Mail class="size-4 shrink-0" />
                        <span> {email} </span>
                    </a>
                    <a
                        href={`tel:${phone.replace(/\s/g, "")}`}
                        class="flex items-center gap-2">
                        <Phone class="size-4 shrink-0" />
                        <span> {phone} </span>
                    </a>
                    <div class="flex items-center gap-2">
                        <MapPin class="size-4 shrink-0" />
                        <span> {address} </span>
                    </div>
                </div>
            </div>
        </div>
        <div
            class="mt-12 pt-8 border-t border-primary-fg/20 text-center text-sm text-primary-fg/50">
            © {new Date().getFullYear()} Diseña Riego. Todos los derechos reservados.
        </div>
    </div>
</footer>
