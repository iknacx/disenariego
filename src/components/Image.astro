---
import { Image as Img } from "astro:assets";

const { src, alt, class: clazz } = Astro.props;
---

<Img
    src={src}
    alt={alt}
    class={clazz}
    widths={[400, 800, 1200]}
    sizes="(min-width: 1024px) 50vw, 100vw"
/>
